import React from 'react'

const Button = ({title}) => {
  return (
    <button className='py-4 px-10 bg-primary text-white border-solid rounded font-man text-base font-bold'>{title}</button>
  )
}

export default Button